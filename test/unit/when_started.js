import { describe, it } from 'mocha';
import start from '../../src/app';

describe('unit', () => {
  describe('account', () => {
    describe('app', () => {
      describe('when started', () => {
        it('should...', () => {
          start();
        });
      });
    });
  });
});
